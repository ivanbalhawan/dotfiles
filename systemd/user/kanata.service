[Unit]
Description=Kanata Keyboard Remapper
After=graphical-session.target

[Service]
Type=simple
# %h expands to /home/youruser
ExecStart=%h/.cargo/bin/kanata --cfg %h/.config/kanata/kanata.kbd
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
